{
  "name": "simple-app",
  "version": "1.0.0",
  "description": "Node project for CI/CD pipeline example",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "echo 'No build step defined'",
    "test": "jest"
  },
  "dependencies": {
    "aws-sdk": "^2.814.0"
  },
  "devDependencies": {
    "jest": "^27.0.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
