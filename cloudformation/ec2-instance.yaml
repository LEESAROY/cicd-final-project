Resources:
  leesa9019432EC2Instance:
    Type: 'AWS::EC2::Instance'
    Properties:
      InstanceType: t2.micro
      ImageId: ami-09e4ba81d75ebeb6a
      SecurityGroupIds:
        - !Ref leesa9019432InstanceSecurityGroup

  leesa9019432InstanceSecurityGroup:
    Type: 'AWS::EC2::SecurityGroup'
    Properties:
      GroupDescription: Enable SSH and HTTP access
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 22
          ToPort: 22
          CidrIp: 0.0.0.0/0
        - IpProtocol: tcp
          FromPort: 80
          ToPort: 80
          CidrIp: 0.0.0.0/0

Outputs:
  InstanceID:
    Value: !Ref leesa9019432EC2Instance
    Description: Instance ID of the EC2 instance
  PublicIP:
    Value: !GetAtt leesa9019432EC2Instance.PublicIp
    Description: Public IP address of the EC2 instance
